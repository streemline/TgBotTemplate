dist: trusty
sudo: false
language: python
python:
  - 3.6
  - 3.6-dev
  - 3.7-dev
  - nightly

install:
  - pip install -r requirements.txt
  - pip install codecov

script:
  - coverage run --source=app -m unittest

after_success:
  - codecov
